{
  "name": "berichtsheft-tool",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "vite --open",
    "build": "npm run fix &&  vite build",
    "preview": "npm run build && vite preview",
    "fix": "prettier --write .",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "prettier": "^3.3.2",
    "vite": "^5.4.1"
  },
  "dependencies": {
    "sass": "^1.79.3"
  },
  "engines": {
    "node": ">=22.9.0"
  }
}
